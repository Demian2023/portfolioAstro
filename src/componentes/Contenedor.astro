---

interface Props {
	id: string;
}

const { id } = Astro.props;

---
<div id={id} class="flex flex-wrap justify-center items-center h-full min-h-screen pt-14 w-full">
    <div class="flex flex-col text-start justify-start px-8 md:px-20 md:w-4/5 lg:w-2/3">
        <slot />
    </div>
</div>